{
  "name": "cib-civievent-widget",
  "version": "4.3.0",
  "description": "CIB CiviEvent Widget plugin",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "pre-commit run --config .pre-commit-config.yaml --all-files"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "@prettier/plugin-php": "^0.24.0"
  }
}
